{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server/dist/index.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd client && npm install && npm run build && cd .. && cd server && npm install && tsc && node dist/index.js",
    "sheeeeesh": "test-deploy",
    "build": "tsc --build"
  },
  "author": "TEAM7",
  "license": "ISC"
}